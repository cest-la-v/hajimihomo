# minimal preset — light: CN/ads split only, no per-service routing
name: minimal
description: "Light rules: ads blocked, China goes direct, everything else proxied. No per-service routing."
target: mihomo
topology: minimal
features:
  ads_block: true
  tracking_block: false
  quic_block: false
  load_balance: false

groups:
  - block/ads
  - direct/cn
